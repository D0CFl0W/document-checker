version: "3.8"

services:
  python_app:
    build: .
    container_name: docs-checker
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=production
      - DEBUG=False
